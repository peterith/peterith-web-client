{
  "name": "peterith-web-app",
  "version": "0.1.0",
  "scripts": {
    "start": "cd server && npm install && npm run build && cd build && node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build && node app.js"
  }
}
